export default function mergeByName(modifiers) {
    var merged = modifiers.reduce(function(merged, ) {
        var existing = merged[.name];
        merged[.name] = existing ? Object.assign(Object.assign(Object.assign({}, existing), ), {}, {
            options: Object.assign(Object.assign({}, existing.options), .options),
            data: Object.assign(Object.assign({}, existing.data), .data)
        }) : ;
        return merged;
    }, {}); // IE11 does not support Object.values

    return Object.keys(merged).map(function(key) {
        return merged[key];
    });
}